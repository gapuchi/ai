# AI Agents

This repo contains the instructions that can be provided to agents. Depending on the agent, the files will need to be linked to specific config locations.

## Set Up

### Cursor

```zsh
ln -s agents $HOME/.cursor/rules
```

### Claude Code

```zsh
ln -s agents/coding-philosphy.mdc $HOME/.claude/CLAUDE.md
```